# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: macos build

on:
  push:
    branches: [ "build-test" ]

jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.25.5'
    - name: Set up node
      uses: actions/setup-node@v6
      with:
        node-version: '22.14.0'
    - name: Set up wails
      run: go install github.com/wailsapp/wails/v2/cmd/wails@latest
    - name: build
      run: wails build
      env:
        CGO_ENABLED: 1
        CGO_CFLAGS:   -I$(pwd)/third_party/onnxruntime-osx-arm64-1.18.1/include
        CGO_LDFLAGS:  -L$(pwd)/third_party/onnxruntime-osx-arm64-1.18.1/lib -lonnxruntime -Wl,-rpath,$(pwd)/third_party/onnxruntime-osx-arm64-1.18.1/lib
